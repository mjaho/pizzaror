<%= form_for(@pizza) do |f| %>
  <% if @pizza.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pizza.errors.count, "error") %> prohibited this pizza from being saved:</h2>

      <ul>
      <% @pizza.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>

    <ul>
      <% @toppings.each do |t| %>
          <li>
            <% if @pizza.toppings.include?(t) %>
                <%= check_box_tag 'topping_ids[]', t.id, true -%>
                <%= h t.name -%>
            <% else %>
                <%= check_box_tag 'topping_ids[]', t.id-%>
                <%= h t.name -%>
            <% end %>
          </li>
      <% end %>
    </ul>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
