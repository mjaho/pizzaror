<%= form_for(@favoritepizza) do |f| %>
  <% if @favoritepizza.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favoritepizza.errors.count, "error") %> prohibited this favoritepizza from being saved:</h2>

      <ul>
      <% @favoritepizza.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :pizza_id %><br>
    <%= f.number_field :pizza_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
